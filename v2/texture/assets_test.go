package texture_test

import ()

func ExampleCheckers() {
	//scene := builder.NewSceneBuilder()
	//tex2D := texture.Checkers(1, 1, color.White, color.Black)
	//tex3D := texture.Map(tex2D, texture.UVProject{})
	//mat := material.Flat(tex3D)
	//rect := builder.NewRectangle(mat, Vec{-1, -1, 0}, Vec{-1, 1, 0}, Vec{1, -1, 0})
	//scene.Add(rect)
	//test.OnePass()
	// Output:

	// exampleCheckers.png
}
